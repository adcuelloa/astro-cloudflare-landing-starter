# Public site URL (overrides astro.config.mjs `site` if set at build time)
PUBLIC_SITE_URL=https://acme.example.com

# Cloudflare R2/CDN custom domain for image masters and /cdn-cgi/image transforms.
PUBLIC_CDN_URL=https://cdn.acme.example.com
